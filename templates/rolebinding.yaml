apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: flask-rolebinding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: flask-sa
    namespace: default
roleRef:
  kind: Role
  name: flask-role
  apiGroup: rbac.authorization.k8s.io
